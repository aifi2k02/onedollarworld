# Copy to .env.local and fill in. .env.local is gitignored — never commit keys.

# Supabase project (URL is public; anon key is public-safe — exposed in the browser).
NEXT_PUBLIC_SUPABASE_URL=https://ssovcsdjbbvvxznejxdy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here

# Production site URL (used for absolute OG/share image links).
NEXT_PUBLIC_SITE_URL=https://onedollarworld.xyz

# DO NOT put the service_role key or the database password in this file or the repo.
# Those are only needed server-side for admin/ingestion jobs, kept out of source control.
